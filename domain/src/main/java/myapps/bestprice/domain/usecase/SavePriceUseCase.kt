package myapps.bestprice.domain.usecase

import myapps.bestprice.domain.repository.ValuesRepository

class SavePriceUseCase(private val valuesRepository: ValuesRepository) {

//    fun execute(inputData: Int) : Boolean {
//        return val result: Boolean = valuesRepository.savePrice(Int)
//    }
}